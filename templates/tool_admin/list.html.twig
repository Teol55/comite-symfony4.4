{% extends 'comite_admin/admin.html.twig' %}





{% block content_body %}
    <a href="{{ path('admin_tool_new') }}" class="btn btn-primary pull-right">
        Nouveau  <span class="fa fa-plus-circle"></span>
    </a>

    <h1> OUTILS</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Non</th>
            <th>Description</th>
            <th>Prix à la semaine</th>
            <th>Modifier</th>
            <th>Supprimer</th>
        </tr>
        </thead>
        <tbody>
        {% for tool in tools %}
            <tr>
                <td>{{ tool.name|upper }}</td>
                <td>{{ tool.description|raw }}</td>
                <td>{{ tool.price  }}€</td>
                <td>
                    <a href="{{ path('admin_tool_edit', {
                        id: tool.id
                    }) }}">
                        <span class="fa fa-pencil"></span>
                    </a>
                </td>
                <td>
                    <a href="{{ path('admin_tool_delete', {
                        id: tool.id
                    }) }}">
                        <span class="fa fa-trash"></span>
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
